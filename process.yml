apps:
  - script: index.js
    name: json-api
    watch: true
    ignore_watch:
      - "data"
      - "node_modules"
    env:
      NODE_ENV: production
      HTTP_PORT: 80
    log_date_format: YYYY-MM-DD HH:mm Z
